apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - traefik
  - metrics-server
  - cloudflare-tunnel
  - local-path-provisioner  # Must deploy before kube-prometheus-stack (needs storage)
  # TODO: Re-enable after infrastructure is ready
  # - kube-prometheus-stack
