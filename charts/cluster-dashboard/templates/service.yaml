apiVersion: v1
kind: Service
metadata:
  name: {{ include "cluster-dashboard.fullname" . }}
  labels:
    {{- include "cluster-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "cluster-dashboard.selectorLabels" . | nindent 4 }}
  sessionAffinity: None
